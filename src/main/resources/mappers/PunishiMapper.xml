<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.library.manage.dao.PunishMapper">

    <insert id="insertPuninsh" parameterType="punish">
        insert into punish(money,reason,record_id,create_time)
        VALUES (#{money} , #{reason}, ${record.getId()} , #{createTime})

    </insert>

</mapper>